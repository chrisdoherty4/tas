# Finding documentation for a previous go version

Can access documentation for previous go versions by appending an `@<version>` 
to the `pkg.go.dev` URL. The version can be `M`, `M.m` or `M.m.p`.

```
pkg.go.dev/<package>@<version>?tab=docs
```

```
pkg.go.dev/runtime@1.14
```